<Window x:Class="Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"        
        xmlns:encodeMe="clr-namespace:Server"
        xmlns:cnv="clr-namespace:Server.Converters"
        xmlns:viewModels="clr-namespace:Server.ViewModels"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        MinWidth="555" MinHeight="471"
        WindowState="Maximized"
        Title="EncodeMe Server" Height="471" Width="555">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Shadows.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ToggleButton.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Menu.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <md:DialogHost Identifier="RootDialog" SnackbarMessageQueue="{Binding ElementName=MainSnackbar, Path=MessageQueue}">
        <md:DrawerHost IsRightDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">
         
            <Grid IsSharedSizeScope="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" SharedSizeGroup="a"/>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <md:ColorZone Padding="7"
                                  x:Name="HeaderColorZone"
                              VerticalAlignment="Stretch"
                                            Mode="PrimaryDark">
                        <Grid Margin="7">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <md:PackIcon Kind="Server" Height="32"
                                     Width="32"
                                     Padding="0" Margin="0"
                                     VerticalAlignment="Center" />
                            <TextBlock Text="EncodeMe Server"
                                       Grid.Column="1"
                                       Visibility="{Binding ElementName=HeaderToggle,Path=IsChecked,Converter={cnv:BooleanToVisibility Collapsed,Visible}}"
                                   Margin="10 0 20 0"
                                       FontWeight="Bold"
                                   FontSize="14pt"
                                   VerticalAlignment="Center" />
                        </Grid>
                    </md:ColorZone>
                    <Grid Grid.Row="1"
                                  VerticalAlignment="Stretch">
                        <Rectangle HorizontalAlignment="Stretch"
                                   Opacity="0.7"
                                   VerticalAlignment="Stretch"
                                   Fill="{Binding ElementName=HeaderColorZone,Path=Background}"/>
                        <ListBox IsSynchronizedWithCurrentItem="True"
                                 x:Name="DemoItemsListBox"
                                 Foreground="{StaticResource MaterialDesignDarkForeground}"
                                 Margin="0 20 0 0"
                                 ItemsSource="{Binding Source={x:Static viewModels:MainViewModel.Instance},Path=Screens}">
                            <ListBox.ItemContainerStyle>
                                <Style BasedOn="{StaticResource MaterialDesignListBoxItem}"
                                       TargetType="ListBoxItem">
                                    <Setter Property="Visibility" Value="{Binding Visibility}"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="viewModels:Screen">
                                    <Grid Margin="7 0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <md:PackIcon Kind="{Binding Icon}"
                                                     Width="24"
                                                     Height="24"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Name}" Margin="32 0 32 0"
                                                   FontSize="11pt"
                                                   Visibility="{Binding ElementName=HeaderToggle,Path=IsChecked,Converter={cnv:BooleanToVisibility Collapsed,Visible}}"
                                                   VerticalAlignment="Center"
                                                   Grid.Column="1" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Grid>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" SharedSizeGroup="a"/>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>


                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ToggleButton Style="{StaticResource MaterialDesignExpanderToggleButton}"
                                      Cursor="Hand"
                                      x:Name="HeaderToggle"
                                      VerticalAlignment="Center"
                                      Margin="20 0">
                            <ToggleButton.LayoutTransform>
                                <RotateTransform Angle="90"
                                                 CenterX="0.5"
                                                 CenterY="0.5"/>
                            </ToggleButton.LayoutTransform>
                        </ToggleButton>

                        <md:Transitioner Grid.Column="1"
                            ItemsSource="{Binding Source={x:Static viewModels:MainViewModel.Instance},Path=Screens}"
                            IsSynchronizedWithCurrentItem="True">
                            <md:Transitioner.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <md:PackIcon Kind="{Binding Icon}"
                                         Height="24"
                                         Width="24"
                                         Margin="10 0"
                                         VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}"
                                       Style="{StaticResource MaterialDesignHeadlineTextBlock}"
                                       VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </md:Transitioner.ItemTemplate>
                            
                        </md:Transitioner>

                        <md:Transitioner Grid.Column="2"
                                         Margin="10 0"
                                         ItemsSource="{Binding Source={x:Static viewModels:MainViewModel.Instance},Path=Screens}"
                                         IsSynchronizedWithCurrentItem="True">
                            <md:Transitioner.ItemTemplate>
                                <DataTemplate>
                                    <ItemsControl ItemsSource="{Binding Commands}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                        Margin="7 0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <md:PackIcon Kind="{Binding Icon}"
                                                             VerticalAlignment="Center" />
                                                        <TextBlock Text="{Binding Name}"
                                                                   Margin="4 0 0 0"
                                                           VerticalAlignment="Center" />
                                                    </StackPanel>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </DataTemplate>
                            </md:Transitioner.ItemTemplate>

                        </md:Transitioner>

                        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0 0 0 1" 
                                Grid.Column="0" 
                                Grid.ColumnSpan="3"/>
                    </Grid>



                    <ScrollViewer Grid.Row="1"
                                  HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto">
                        <md:Transitioner ItemsSource="{Binding Source={x:Static viewModels:MainViewModel.Instance},Path=Screens}"
                                         IsSynchronizedWithCurrentItem="True">
                            <md:Transitioner.ItemTemplate>
                                <DataTemplate>
                                    <ContentControl Content="{Binding Content}"/>
                                </DataTemplate>
                            </md:Transitioner.ItemTemplate>
                        </md:Transitioner>
                    </ScrollViewer>

                    <md:Snackbar MessageQueue="{Binding Source={x:Static viewModels:MainViewModel.Instance},Path=MessageQueue}" x:Name="MainSnackbar"
                                 VerticalAlignment="Bottom"
                                 Grid.Row="1" />
                </Grid>
            </Grid>
        </md:DrawerHost>
    </md:DialogHost>
</Window>
